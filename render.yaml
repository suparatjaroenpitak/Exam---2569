services:
  - type: web
    name: online-exam-practice-system
    runtime: node
    plan: free
    region: singapore
    buildCommand: python3 -m ensurepip --upgrade && python3 -m pip install --upgrade pip && python3 -m pip install --target .render-python -r ai_engine/requirements.txt && python3 -c "import sys; sys.path.insert(0, '.render-python'); import huggingface_hub; print(huggingface_hub.__version__)" && npm install && npm run build
    startCommand: npm run start
    autoDeployTrigger: commit
    healthCheckPath: /login
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: PYTHON_EXEC
        value: python3
      - key: PYTHONPATH
        value: .render-python
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: DATA_DIR
        value: data
      - key: JWT_SECRET
        generateValue: true
      - key: HUGGINGFACE_API_KEY
        sync: false
      - key: TRANSFORMERS_MODEL
        value: Qwen/Qwen2.5-1.5B-Instruct
      - key: TRANSFORMERS_MAX_NEW_TOKENS
        value: "1400"
      - key: TRANSFORMERS_TEMPERATURE
        value: "0.95"
      - key: DEFAULT_ADMIN_EMAIL
        sync: false
      - key: DEFAULT_ADMIN_PASSWORD
        sync: false

# If you move to a paid Render plan later, you can attach a persistent disk and
# mount it at /opt/render/project/src/data without changing the application code.